/**
 * Angry Tiger — shadow tokens.
 * Not specified in the Brand Guidelines; flat poster surfaces are the
 * house style, and shadows are used sparingly — only where something
 * needs to read as physically floating (the membership card, elevated
 * dark cards on black).
 */

export const shadows = {
  poster: "0 24px 60px rgba(0, 0, 0, 0.55)",
  card: "0 8px 24px rgba(0, 0, 0, 0.4)",
} as const;
