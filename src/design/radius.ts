/**
 * Angry Tiger — radius tokens.
 * The Brand Guidelines are hard-edged throughout — logos, cards, layouts,
 * and every mockup use square corners with no rounding. So the system has
 * exactly one radius: none. There are no rounded elements on the site.
 */

export const radius = {
  none: "0px",
} as const;
